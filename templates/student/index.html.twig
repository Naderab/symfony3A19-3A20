<table border="1">
    <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Moyenne</th>
        <th>Classroom</th>

    </tr>
    {% for c in students %}
    <tr>
        <td>{{c.id}}</td>
        <td>{{c.name}}</td>
        <td>{{c.moyenne}}</td>
        <td>{{c.idClassroom.username}}</td>
    </tr>
    {%endfor %}
</table>