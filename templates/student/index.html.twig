{% extends 'base.html.twig' %}

{% block title %}Students list!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="container">
<div class="row">
<div class="col-md-4">
        <a class="btn btn-primary" href="{{path('Student_add')}}">Add new student</a>

</div>

</div>
<div class="row">
    <div class="col-md-4">
{{form(form)}}    
    </div>
    
    </div>
<div class="row">
    <table class="table table-striped" >
        <thead>
            <tr>
            <th>NSC</th>
            <th>Name</th>
            <th>Moyenne</th>
            <th>Classroom</th>
        
            </tr>
            </thead>
            <tbody>
{%for s in students%}
    <tr>
        <td>{{s.id}}</td>
        <td>{{s.name}}</td>
        <td>{{s.moyenne}}</td>
        <td>{{s.idClassroom.username}}</td>
        <td><a class="btn btn-danger" href="{{path('student_delete',{'id':s.id})}}">Delete</a></td>
        <td><a class="btn btn-primary" href="{{path('Student_update',{'id':s.id})}}">Update</a></td>

    </tr>
</tbody>
{%endfor%}
</table>
</div>
</div>
{%endblock%}